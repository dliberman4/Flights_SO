include ../Makefile.inc

DATABASE_OBJ=$(wildcard ../database/*.o)

DATABASE_TESTS_SRC=database.c
DATABASE_TESTS=$(DATABASE_TESTS_SRC:.c=.test)

tests: $(DATABASE_TESTS)

$(DATABASE_TESTS): $(DATABASE_TESTS_SRC)
	$(CC) $(CC_FLAGS) $(DATABASE_TESTS_SRC) $(DATABASE_OBJ) -o $(DATABASE_TESTS)
